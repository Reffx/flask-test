import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
from io import BytesIO
import base64

# Mocked data for feature importance
def createHeatmapMockedData():
    # Generate example feature names
    feature_names = ['Feature A', 'Feature B', 'Feature C', 'Feature D', 'Feature E']

    # Generate example importance scores (you can replace these with your own values)
    importance_scores = np.array([0.4, 0.3, 0.2, 0.15, 0.1])

    # Call the function to generate the heatmap
    image_base64 = generate_feature_importance_heatmap(feature_names, importance_scores)

    return image_base64
